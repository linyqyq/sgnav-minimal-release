name: usv-clip
channels:
  - defaults
dependencies:
  - python=3.10.8
  - pip
  - pip:
      - numpy==1.21.2
      - torch==2.5.1+cu121
      - torchvision==0.20.1+cu121
      - mlagents-envs==0.30.0
      - protobuf==3.20.3
      - transformers
      - open-clip-torch
      - pillow
      - matplotlib
      - timm
      - tqdm
      - pyyaml
      - requests

