# Minimal Release Scope

This public package intentionally contains only a representative subset of the full paper workspace:

- one Linux Unity harbour build,
- one SGNav Task-1 checkpoint,
- policy evaluation code,
- runtime profiling code,
- example profiling summaries.

It does not include the Unity Editor project, full training logs, all ablation checkpoints, or all paper scenes.

